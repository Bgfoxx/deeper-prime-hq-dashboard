# Path to the data directory (synced via Syncthing between machines)
# Main Mac:    DATA_DIR=/users/ivansmacbook/Public/Transfer Folder/deeper-prime-data
# OpenClaw Mac: DATA_DIR=/users/gateway/Documents/Synced/deeper-prime-data
DATA_DIR=

# Google Calendar OAuth (optional — for calendar integration)
# Create credentials at https://console.cloud.google.com/apis/credentials
# Authorized redirect URI: http://localhost:3000/api/calendar/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3000/api/calendar/callback

# Telegram Bot (optional — for Daily Agenda delivery)
# Create a bot via @BotFather, then get your chat ID via @userinfobot
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
